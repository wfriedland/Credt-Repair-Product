using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using System.ComponentModel.DataAnnotations;
using WebApplication3.FilterData;

namespace WebApplication3
{
    public class DisplayResults
    {
        Debt debt = new Debt();
        QueryDB query = new QueryDB();
        /*
        public List<Debt> DisplayResults(Guid userId)
        {            
            // Returns all debt old & new
            return ;
        }
        public List<Debt> DisplayResults(Guid userId, DateTime date)
        {
            Debt debt = new Debt();
            // Returns all debt for a "ran on" date
        }
        public List<Debt> DisplayResults(Guid userID, DateTime date, string goodOrBad)
        {
            // Returns all debt for a "ran on & bad debt"
        }
         */
    }
}