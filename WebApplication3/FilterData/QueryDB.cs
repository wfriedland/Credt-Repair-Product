using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace WebApplication3.FilterData
{
    public class QueryDB
    {
        /*
        public Array[] QueryDB(Guid userId)
        {
            //Querry the Database and return everything array 
        }
        public Array[] QueryDB(Guid userId, DateTime reportRanDate)
        {
            //Querry the Database and return specific "report ran on date" array 
        }
        public Array[] QueryDB(Guid userId, DateTime reportRanDate, string goodOrBadCredit)
        {
            //Querry the Database and return bad debt on report ran on date array 
        }
         */
        
    }
}