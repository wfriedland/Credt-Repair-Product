using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace WebApplication3
{
    public class ConvertToClass
    {
        //Once the DB is set up create class: Debts.
        /*
        public Debt ConvertToClass(List<string> userdebt)
        {
            Debt debt = new Debt();

            //debt.accountNumber = userdebt[1];
            // match up all of the class prop to DB fields
            return debt;
        }
         */
    }
}